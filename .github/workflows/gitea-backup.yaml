name: Gitea Backup

on:
  - workflow_dispatch

jobs:
  demo:
    name: Demo Job
    runs-on: ubuntu-24.04
    steps:
      - name: Send Telegram Message
        uses: y-developer/my-cron-tab/.github/actions/tg-send-message@main
        with:
          message: "This is a demo message from the Gitea Backup workflow."
